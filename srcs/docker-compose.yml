version: "3"
services:
    nginx:
        build: ./requirements/nginx
        image: nginx
        container_name: nginx
        ports:
            - 443:443
        networks:
            inception:
    
    wordpress:
        build: ./requirements/wordpress
        image: wordpress
        container_name: wordpress
        ports:
            - 9000:9000
        networks:
            inception:
networks:
    inception: